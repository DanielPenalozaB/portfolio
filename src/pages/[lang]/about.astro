---
/**
 * TODO: About page
 * Future structure:
 * - Hero with profile photo and short intro
 * - Personal bio / story section
 * - Professional timeline (education, career milestones)
 * - Interests & hobbies
 * - Contact form (serverless backend)
 */

import { languages } from "../../i18n";

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({ params: { lang } }));
}

return Astro.redirect("/");
---
